# Open questions

## When (if ever) should extensions graduate?

If lore-explorer (or future tools) become widely used, should they stay under `extensions/` indefinitely, move to a separate repository, or join a broader tooling package? Prefer evidence from dogfooding over premature promotion to the root.
